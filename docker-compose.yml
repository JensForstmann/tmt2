# ------------------------------------------------
# DO NOT EDIT this file, but create a
# "docker-compose.override.yml" for local changes.
# ------------------------------------------------

services:
  tmt2:
    build:
      dockerfile: dev.Dockerfile
    # environment:
    # - TZ=Europe/Berlin
    volumes:
      - .:/app
    ports:
      - '0.0.0.0:8080:8080'
    working_dir: /app/backend
    command: npm run dev
