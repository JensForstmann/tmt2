{
	"name": "tmt2",
	"version": "2.0.0",
	"description": "TMT is a tool that tracks/watches/observes a Counter-Strike 2 match.",
	"repository": "github:JensForstmann/tmt2",
	"author": "Jens Forstmann",
	"license": "MIT",
	"scripts": {
		"build": "cd backend && npm run build && cd ../frontend && npm run build",
		"dev": "cd backend && npm run dev & cd frontend && npm run dev",
		"clean": "prettier '!**/*md' . --write",
		"prettier-check": "prettier '!**/*md' . --check"
	},
	"devDependencies": {
		"prettier": "^3.6.2",
		"prettier-plugin-tailwindcss": "^0.7.1"
	}
}
